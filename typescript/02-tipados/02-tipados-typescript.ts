// Tipos básicos
let nombre: string = "Estudiante";
let edad: number = 25;
let esNuevo: boolean = true;

// Tipos especiales
let cualquierValor: any = "Esto puede cambiar a cualquier tipo";
let indefinido: undefined = undefined;
let nulo: null = null;

// Array
let listaNumeros: number[] = [1, 2, 3, 4, 5];

// Tuple (Tupla)
let tupla: [string, number] = ["Hola", 42];

// Literales
let direccion: "izquierda" | "derecha" | "arriba" = "arriba";

console.log(direccion);
